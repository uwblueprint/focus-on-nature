import React from "react";
import { Box, Text } from "@chakra-ui/react";

const ScheduleSessionsPage = (): JSX.Element => {
  return (
    <Box>
      <Text textStyle="displayXLarge">Schedule Sessions</Text>
    </Box>
  );
};

export default ScheduleSessionsPage;
